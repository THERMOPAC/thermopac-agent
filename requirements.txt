# ThermopacAgent — Windows only
# Install with: pip install -r requirements.txt

# SolidWorks COM automation (Windows only)
pywin32>=306

# HTTP client
requests>=2.31.0

# PyInstaller for building the EXE
pyinstaller>=6.0.0
